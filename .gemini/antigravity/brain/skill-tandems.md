# Effective Skill Tandems

## Authentication & RBAC
- `@auth-implementation-patterns` + `@api-security-best-practices` + `@backend-security-coder` worked together to ensure robust and cryptographically secure user logins.

## Database & POS
- `@database-design` + `@postgres-best-practices` + `@senior-fullstack` ensured relational integrity, transactional safety, and optimal indexing for the core sales and inventory tables.

## Front-End UI
- `@ui-ux-pro-max` + `@frontend-design` + `@react-patterns` + `@tailwind-patterns` ensured exact alignment with customized theme variables, WCAG AA compliance, and modular component reusability.
